name: build

# This action works with pull requests and pushes
on:
  pull_request:
  push:
    branches:
      - main

jobs:
  tests:
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        build: [msrv, stable, beta, nightly]
        os: ["ubuntu-latest", "macOS-latest", "windows-latest"]
        config: ["termion", "crossterm", "console", "default", "all"]

    steps:
      - name: Checkout
        uses: actions/checkout@v2
        with:
          # Make sure the actual branch is checked out when running on pull requests
          ref: ${{ github.head_ref }}

      - name: Define Rust version
        run: |
          if [ "${{ matrix.build }}" = "msrv" ]; then
            echo '1.58.1' >> $RUST_VERSION
          else
            echo '${{ matrix.build }}' >> $RUST_VERSION
          fi

      - name: Install toolchain
        uses: actions-rs/toolchain@v1
        with:
          toolchain: ${{ env.RUST_VERSION }}
          override: true
          profile: minimal
          components: rustfmt, clippy

      - name: Cache cargo files
        uses: actions/cache@v2
        with:
          path: |
            ~/.cargo/bin
            ~/.cargo/.crates.toml
            ~/.cargo/.crates2.json
            ~/.cargo/registry/index
            ~/.cargo/registry/cache
            ~/.cargo/git/db
            ./target
            ./test-target
          key: ${{ runner.os }}-cargo-files-${{ hashFiles('**/Cargo.lock', '**/Cargo.toml') }}
          restore-keys: |
            ${{ runner.os }}-cargo-files-

      - name: Define features
        run: |
          if [ "${{ matrix.config }}" = "termion" ]; then
            echo "--no-default-features --features=termion" >> $FEATURES
          elif [ "${{ matrix.config }}" = "crossterm" ]; then
            echo "--no-default-features --features=crossterm" >> $FEATURES
          elif [ "${{ matrix.config }}" = "console" ]; then
            echo "--no-default-features --features=console" >> $FEATURES
          elif [ "${{ matrix.config }}" = "default" ]; then
            echo "" >> $FEATURES
          elif [ "${{ matrix.config }}" = "all" ]; then
            echo "--all-features" >> $FEATURES
          fi

      - name: Test
        run: cargo test --verbose ${{ env.FEATURES }}

      - name: Check format
        run: cargo fmt --check

      - name: Check lint
        run: cargo clippy -- -D warnings
